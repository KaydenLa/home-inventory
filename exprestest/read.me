This is the backend.
This uses Express.JS to host the backend.
This is hosted on port 3000.
To start this portion of the backend, run nodemon index.JS
Not added yet, but evenutally this should connect to MongoDB to hold the raw data.
